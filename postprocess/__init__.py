from postprocess.extract_redundant_words import *
from postprocess.normalize import *

extract_redundant_words = extract_redundant_words(0, "extract_redundant_words",1)
normalize = normalize(0, "normalize", 2)
